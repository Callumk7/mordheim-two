# Copy to .dev.vars beside this file (gitignored). No key is needed while disabled.
IMAGE_GENERATION_ENABLED=false
GEMINI_API_KEY=
# WARNING: true + a real key makes PAID network calls even in local development.
